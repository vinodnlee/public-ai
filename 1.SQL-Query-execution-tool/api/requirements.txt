# ── Core Framework ───────────────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0

# ── Database ─────────────────────────────────────────────────────────────────
# 2.0.31+ required for Python 3.13 (TypingOnly / __static_attributes__ fix)
sqlalchemy[asyncio]>=2.0.31,<2.1
asyncpg>=0.30.0         # PostgreSQL async driver (0.30+ has py313 win wheels)
aiomysql==0.2.0          # MySQL async driver
aiosqlite==0.20.0        # SQLite async driver

# ── Caching / Session ────────────────────────────────────────────────────────
redis==5.0.3

# ── Config / Validation ──────────────────────────────────────────────────────
# Use >=2.10 for Python 3.13 pre-built wheels (avoid Rust build)
pydantic>=2.10.0,<3
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# ── Auth ─────────────────────────────────────────────────────────────────────
PyJWT==2.8.0
passlib[bcrypt]==1.7.4

# ── Streaming ────────────────────────────────────────────────────────────────
sse-starlette==1.8.2

# ── LangChain + DeepAgent (pinned to avoid resolution-too-deep) ─────────────────────
deepagents==0.4.1
langchain>=1.2.10,<2.0.0
langchain-core>=1.2.10,<2.0.0
langchain-openai>=0.2.0
langgraph>=1.0.0,<2.0.0
openai>=1.30.1,<2.0.0
httpx>=0.27.0

# ── Dev / Test ────────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-asyncio==0.23.6
black==24.3.0
ruff==0.3.4
mypy==1.9.0
